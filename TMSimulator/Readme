
   To make a new build for the TM Simulator, type "make clean" and "make". 

   To run a compiled program such as "sort.tm", type "./tm sort.tm".  Once in the simulator, type "h" for the list of available commands.  In particular, type "g" to run the program, and the output should be displayed on the screen. To run the program again, type "c" to clear up the environment and type "g" to run the program.

   To quit the simulator, simply type "q" to exit.
